# 세션 회고 — Todo 앱 구축

2026-07-20, PRD 작성부터 앱 완성 및 디자인 개편까지 진행한 세션 기록.

## 진행 흐름

1. PRD 기반 DB 스키마 설계 (`todos` / `tags` / `todo_tags` / `reminders` 4테이블) → ER 다이어그램 아티팩트 + [DB_SCHEMA.md](DB_SCHEMA.md)로 정리
2. 계획 모드로 기술스택(Node.js + Express + 바닐라 JS + better-sqlite3)과 알림 방식(브라우저 알림 + 목록 강조)을 먼저 확정 → 6단계 계획 승인 후 순서대로 구현
3. 각 단계 끝날 때마다 실제로 동작 확인 (API는 curl, UI는 브라우저 자동화로 클릭·타이핑까지)
4. 앱 완성 후 두 차례 UI 개선 — 입력 라벨 명확화 → 첨부 레퍼런스를 참고한 타임라인/FAB 전면 리디자인

## 잘 된 점

- "말로 설명" 대신 매번 실제로 실행해서 확인 — API 레벨과 브라우저 레벨을 이중으로 검증
- 계획 단계에서 애매한 지점(스택, 알림 방식)을 먼저 질문으로 확정해서 중간에 뒤엎는 일 없이 진행
- 리디자인 때 중복되던 CSS(`reminder-field`)를 `labeled-field`로 일반화해서 재사용

## 삽질했던 것들 (앱 버그 아님, 테스트 과정의 함정)

- Git Bash에 한글을 커맨드라인으로 바로 넘기면 인코딩이 깨짐 → 파일로 써서 `--data-binary`로 전송하는 방식으로 우회
- 브라우저 자동화 `form_input`이 `datetime-local`에 타임존 변환을 적용해 날짜가 하루 밀려 보임 → DOM 직접 조작으로 재현해서 앱 코드는 정상임을 확인

## 실제로 발견해서 고친 버그 2건

- `.modal-backdrop { display:flex }`가 `[hidden]` 속성보다 우선 적용돼 모달이 첫 로드부터 열려 보이던 문제
- day-pill 버튼에 스크린리더용 접근 가능한 이름이 없던 접근성 누락

## 실수 1건

- 리디자인 테스트 중 사용자가 직접 만든 "AI Agent 과제" 항목을 실수로 완료 처리 → 발견 즉시 되돌림

## 현재 상태

- `http://localhost:3000`에서 실행 중, `todo.db`에 사용자 데이터 보존됨
- 태그는 생성 시 파스텔 색이 자동 배정되어 DB에 저장(기존 태그도 1회성 보정 완료)
- `.env`/`todo.db`는 `.gitignore` 처리돼 있지만 git 저장소 자체는 아직 초기화 안 함 (요청받은 적 없어서 그대로 둠)

## 참고만 해두면 좋은 것 (결정 아님)

- day-strip이 "이번 주"만 보여줘서 다음 주 이후 마감일은 날짜 버튼으론 못 짚음 (검색/태그 필터는 여전히 가능)
- 알림 폴링은 브라우저 탭이 열려 있어야 동작 — 로컬 앱 특성상 자연스러운 한계
