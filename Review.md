# Code Peer Review — my-todo

- 코더 : 최주희
- 리뷰어 : 신서연

## PRT(Peer Review Template)

[x] **1. 주어진 문제를 해결하는 완성된 코드가 제출되었나요?**
- PRD(`Todo 앱 구현 계획.md`)가 요구한 CRUD, 마감일, 카테고리(태그), 오늘 할 일만 보기, 검색, 마감일 알림이 전부 구현됨.
  - CRUD: `server.js:97-245` (GET/POST/PATCH/DELETE `/api/todos`)
  - 태그 다대다: `server.js:72-93` (`findOrCreateTag`, `setTodoTags`)
  - 오늘 할 일: `server.js:102-103` (`today=1` 쿼리)
  - 검색: `server.js:112-115` (`LIKE` 파라미터 바인딩)
  - 마감일 알림: `reminders` 테이블 + `/api/reminders/due` 폴링 (`server.js:276-295`, `public/app.js:354-372`)
- 리뷰 과정에서 실제로 `npm install` → `npm start` → API 호출까지 재구동해서 CRUD, 태그 필터, 오늘 할 일 필터, 검색, 완료 토글, 알림 등록/폴링(`is_sent` 중복 방지 포함), 삭제를 모두 직접 확인함.
- `RETROSPECTIVE.md:9`에도 "API는 curl, UI는 브라우저 자동화로 클릭·타이핑까지" 검증했다는 기록이 있고, `DB_SCHEMA.md`의 "PRD 기능 ↔ 테이블 매핑" 표로 요구사항과 구현이 1:1로 매칭됨을 스스로 확인해둠.

[x] **2. 핵심적이거나 복잡하고 이해하기 어려운 부분에 작성된 설명을 보고 해당 코드가 잘 이해되었나요?**
- `server.js:64` — "색 지정 기능 추가 전에 만들어진 태그를 위한 1회성 보정"처럼, 코드가 *왜* 존재하는지(마이그레이션성 보정)를 짧게 설명한 주석이 있어 의도 파악에 도움이 됨.
- `DB_SCHEMA.md`의 "설계 이유" 섹션(태그를 별도 테이블로 둔 이유, reminders를 분리한 이유, users 테이블을 안 둔 이유)이 아키텍처 결정의 근거를 잘 설명함.
- (개선 여지) `attachRelations`가 태그/알림을 N+1 쿼리 대신 배치 조회로 붙이는 이유, `getMonday`의 주차 계산 로직 등 일부 핵심 헬퍼에는 "왜 이렇게 짰는지" 주석이 없음 — 코드 자체로 읽어서 이해는 가능한 수준이라 큰 감점 요인은 아님.

[x] **3. 에러가 난 부분을 디버깅하여 "문제를 해결한 기록"을 남겼나요? 또는 "새로운 시도 및 추가 실험"을 해봤나요?**
- `RETROSPECTIVE.md:23-27` "실제로 발견해서 고친 버그 2건": `.modal-backdrop{display:flex}`가 `[hidden]` 속성보다 우선 적용되어 모달이 첫 로드부터 열려 보이던 문제, day-pill 버튼의 스크린리더 접근성(aria-label) 누락 — 원인과 해결이 명확히 기록됨.
- `RETROSPECTIVE.md:20-21` "삽질했던 것들" — Git Bash 한글 커맨드라인 인코딩 문제를 파일+`--data-binary` 전송으로 우회, 브라우저 자동화의 `datetime-local` 타임존 변환 버그를 DOM 직접 조작으로 재현해서 "앱 코드는 정상"임을 별도로 검증한 기록까지 남김.
- (리뷰 중 동일 함정 재확인) 리뷰어가 태그 필터를 curl로 테스트하던 중 한글 인자를 커맨드라인에 직접 넘겨 인코딩이 깨진 것을 "앱 버그"로 오인할 뻔했으나, DB를 직접 열어 바이트 단위로 대조해 SQL 로직 자체는 정상임을 확인함 — `RETROSPECTIVE.md`가 이미 문서화해둔 것과 동일한 함정이라 실제 결함이 아님으로 결론.

[x] **4. 회고를 잘 작성했나요?**
- `RETROSPECTIVE.md` — 잘된 점, 삽질(테스트 과정의 함정), 실제로 고친 버그 2건, 실수 1건(테스트 중 사용자의 실제 데이터를 실수로 완료 처리했다가 즉시 복구), 현재 상태, 참고사항까지 항목별로 구조화되어 배운점·아쉬운점이 상세히 드러남.
- 딥러닝 프로젝트가 아니므로 모델 아키텍처 도식화는 해당 없음. 대신 `DB_SCHEMA.md`의 mermaid ER 다이어그램이 데이터 흐름(입력→저장 구조) 이해를 돕는 역할을 대신함.

[x] **5. 코드가 간결하고 효율적인가요?**
- (PEP8은 Python 대상 항목이라 본 프로젝트(Node.js/JS)에는 미해당 — 대신 일관된 코드 스타일 여부로 확인)
- 태그/알림 조회를 todo마다 반복하지 않고 `getTagsForTodoIds`/`getRemindersForTodoIds`로 배치 조회해 N+1 쿼리를 피함 (`server.js:14-30`).
- `findOrCreateTag`/`setTodoTags`로 태그 생성·연결 로직을 모듈화해 POST/PATCH 라우트에서 재사용 (`server.js:72-93`).
- 프론트엔드도 `api()` 공통 fetch 래퍼(`public/app.js:47-58`)로 에러 처리를 일관되게 통일.

## 평가 루브릭

**1. 보안을 스스로 점검할 수 있습니다** — 통과.
모든 쿼리가 파라미터 바인딩되어 SQL 인젝션이 없고(`server.js` 전체), 프론트에서 사용자 입력은 `textContent`로 렌더링해 XSS도 없음(`public/app.js`). `.env`는 `.gitignore` 처리되어 비밀값 노출도 없음. 다만 PRD/README/RETROSPECTIVE 어디에도 "보안을 검토했다"는 명시적 기록이 없어, 실제 보안은 양호하지만 검토 과정을 문서로 남기면 더 좋았을 것.

**2. 실제로 동작하는 백엔드 동작이 의도한 것과 일치합니다** — 통과.
PRD의 요구 기능과 실제 라우트가 1:1로 매칭되고, `DB_SCHEMA.md`가 이를 스스로 표로 검증해둠. 리뷰 과정에서 서버를 직접 재구동해 CRUD·태그·검색·오늘 할 일·알림 폴링을 API 레벨에서 전부 재현 확인함.

**3. 프론트엔드와 백엔드가 적절하게 만들어져 동작합니다** — 통과.
FAB(플로팅 버튼)+모달+타임라인 구조가 직관적으로 설계되어 있고, `RETROSPECTIVE.md`에 브라우저 자동화로 클릭·타이핑까지 검증한 기록이 있음.

## 참고 링크 및 코드 개선

개선 제안 (통과에는 영향 없음, 참고용):

1. **SQL 쿼리 중복** — todo 단건 조회용 `SELECT id, title, description, due_date AS dueDate, ...` 쿼리문이 `server.js:135-146`(GET 단건), `175-183`(POST 응답), `228-236`(PATCH 응답) 세 곳에 그대로 반복됨. `getTodoById(id)` 같은 헬퍼로 뽑으면 더 간결해질 것 같습니다.
2. **보안 검토 기록** — 실제 코드는 안전하지만, PRD/README/RETROSPECTIVE 어디에도 "보안을 검토했다"는 명시적 언급이 없어서, 나중에 다른 프로젝트에서도 이 점을 회고에 한 줄 남겨두면 평가자가 검토 여부를 더 쉽게 확인할 수 있을 것 같습니다.
3. **네이티브 의존성 안내** — `better-sqlite3`는 네이티브 모듈이라, Windows에서 Visual Studio Build Tools(또는 호환 prebuilt 바이너리)가 없는 환경에서는 `npm install`이 `node-gyp` 단계에서 실패할 수 있습니다(리뷰 중 실제로 재현됨 — Node 24용 prebuilt가 아직 없어 컴파일로 넘어가면서 실패). README에 "Windows에서 빌드 오류 시 Visual Studio Build Tools(C++ 워크로드) 설치 필요" 같은 한 줄을 추가해두면 다른 환경에서 클론한 사람이 겪을 수 있는 첫 삽질을 줄여줄 것 같습니다.
4. **달력(월간 캘린더) 뷰 추가** — 지금은 `day-strip`이 "이번 주"만 보여줘서 다음 주 이후 마감일은 날짜 버튼으로 못 짚는다는 한계를 코더 본인도 `RETROSPECTIVE.md:40`에 이미 적어뒀습니다. `due_date`엔 이미 인덱스(`idx_todos_due_date`)가 있으니, 월 단위로 날짜를 넘기며(◀▶) 그 달의 `due_date` 존재 여부를 점으로 표시하고, 날짜 클릭 시 `?date=YYYY-MM-DD`로 필터링하는 캘린더를 day-strip 위(또는 대체)에 추가하면 이 한계를 해결하면서 기존 `GET /api/todos?date=`를 그대로 재사용할 수 있습니다.
5. **반복 일정 자동 추가 기능** — 매주/매일 반복되는 할 일(예: "월요일마다 운동")을 매번 손으로 새로 추가해야 하는 점이 아쉽습니다. `reminders`를 `todos`와 분리한 것과 같은 설계 원칙으로, `recurring_todos`(title, 반복 규칙 — 예: "매주 월요일" / "매월 1일" 등, 시작일) 같은 별도 테이블을 두고, 서버 기동 시나 매일 자정 배치로 "오늘 생성해야 할 반복 일정"을 확인해 `todos`에 실제 행을 생성해주는 방식을 제안합니다. `todos` 테이블에 `recurring_todo_id` FK 컬럼을 하나 추가하면 "이 할 일이 어느 반복 일정에서 나왔는지" 추적도 가능해집니다.
